<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 仅用于应用内检查 GitHub 更新与下载 APK；核心计时与统计全部本地运行 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 应用内直达“忽略电池优化”授权对话框；仅侧载分发，不受应用商店政策限制 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 声明可见“带桌面图标的应用”（Android 11+ 包可见性）：
         仅用于把使用统计/前台感知里的包名解析成真实应用名（如 com.tencent.mm→微信），
         不是权限，无需用户授权，也拿不到任何额外数据。 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 应用内检查 GitHub Release 并下载 APK 后唤起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 仅用于本机统计各应用使用时长；Play 保护权限，需用户在设置中手动授权 -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AntiLazy">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 通知详情页：独立任务栈，关闭后回到用户之前的应用；不进最近任务 -->
        <activity
            android:name=".ReminderActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:taskAffinity=""
            android:theme="@style/Theme.Reminder" />

        <activity
            android:name=".UsageStatsActivity"
            android:exported="false"
            android:label="@string/usage_title" />

        <activity
            android:name=".WhitelistActivity"
            android:exported="false"
            android:label="@string/whitelist_title" />

        <service
            android:name=".MonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuous anti-laziness reminder while the device is unlocked." />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 健康 watchdog：尝试恢复服务，但不伪造服务死亡期间的使用进度 -->
        <receiver
            android:name=".TickReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>
